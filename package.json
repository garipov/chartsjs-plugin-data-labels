{
  "name": "chartsjs-plugin-data-labels",
  "version": "0.0.0",
  "description": "Plugin for ChartJS to draw labels on charts",
  "main": "src/index.js",
  "scripts": {
    "build": "gulp lint && gulp build",
    "prepublish": "npm run build",
    "watch": "gulp build && gulp watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "chartsjs",
    "plugin",
    "labels"
  ],
  "author": "Bpium",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^13.3.0",
    "eslint": "^3.13.1",
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.1",
    "gulp-eslint": "^3.0.1",
    "gulp-insert": "^0.5.0",
    "gulp-streamify": "^1.0.2",
    "gulp-uglify": "^2.0.0",
    "gulp-util": "^3.0.8",
    "vinyl-source-stream": "^1.1.0"
  },
  "dependencies": {
    "chart.js": "^2.4.0",
    "lodash.defaults": "^4.2.0"
  }
}
